[build]
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Runs daily at 06:00 UTC — drops users whose payment grace period has expired
[functions."expire-failed-payments"]
  schedule = "0 6 * * *"
